# ── Local development ──────────────────────────────────────────────────────────
# Copy this file to .env.local for local overrides (git-ignored).
# On Render, set these via Dashboard → Environment → Add Environment Variable.

REACT_APP_API_BASE_URL=http://localhost:8000

# Example for Render production:
# REACT_APP_API_BASE_URL=https://cyberpulse-api.onrender.com
